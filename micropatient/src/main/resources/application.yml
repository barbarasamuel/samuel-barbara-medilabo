server:
  port: 8999

jwt:
    secret: "secret-key"

spring:
  cloud:
    gateway:
      routes:
        - id: patient-service
          uri: http://localhost:8999
          predicates:
            - Path=/patients/**
        - id: historique-service
          uri: http://localhost:8998
          predicates:
            - Path=/hist/**
        - id: risque-service
          uri: http://localhost:8997
          predicates:
            - Path=/risques/**

  security:
    oauth2:
      resource-server:
        jwt: secret-key
        #  issuer-uri: https://medilabo.org/