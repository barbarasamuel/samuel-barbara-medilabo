server:
  port: 8999

spring:
    profiles:
      active: secret
    application:
      name: micropatient
    datasource:
      #url: jdbc:mysql://localhost:3306/medilabo
      url: jdbc:mysql://mysql:3306/medilabo
      username: root
      password: rootroot_2024
      driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect
          format_sql: true

